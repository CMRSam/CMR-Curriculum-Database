@model CMR_Curriculum_Database.Models.company_map

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>
@if(Request.IsAuthenticated)
{
<div>
    <h4>company_map</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.company_list.Company_Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.company_list.Company_Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.content.Module_Name___CURRENT)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.content.Module_Name___CURRENT)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.CompanyMapID }) |
    @Html.ActionLink("Back to List", "Index", new { searchString = Model.company_list.Company_Name })
</p>
}
else
{
<div>
    @Html.ActionLink("You must login to use this database", "Login", "Account")
</div>

}
