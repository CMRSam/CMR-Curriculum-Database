@model CMR_Curriculum_Database.Models.parent_course

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
@if (Request.IsAuthenticated)
{

    using (Html.BeginForm())
    {
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>parent_course</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Parent_Course_ID)

        <div class="form-group">
            @Html.LabelFor(model => model.Parent_Course_Name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Parent_Course_Name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Parent_Course_Name, "", new { @class = "text-danger" })
            </div>
        </div>
        S
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
    }

<div>
    @Html.ActionLink("Back", "Details", new { id = Model.Parent_Course_ID })
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
}
else
{
< div >
    @Html.ActionLink("You must login to use this database", "Login", "Account")
</ div >
}
</div>